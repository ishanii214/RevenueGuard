# RevenueGuard — Python dependencies
#
# Dependencies are added only when a development phase actually requires
# them (see README — Project Status).

# Phase 2: XGBoost recovery baseline
numpy==1.26.4
pandas==2.3.3
scikit-learn==1.8.0
xgboost==3.2.0

# Phase 3: LangGraph investigation agent (fully deterministic; no LLM)
langgraph==1.1.3
pydantic==2.13.4

# Phase 4: advisory LLM narration — intentionally zero new dependencies
# (OpenAI-compatible endpoints via the standard library; see agent/llm.py)

# Phase 6: PostgreSQL persistence + FastAPI service layer
# (fastapi/uvicorn raised to versions compatible with the mcp SDK's
# starlette requirement in Phase 8)
psycopg[binary]==3.2.3
fastapi==0.141.1
uvicorn==0.52.4
httpx==0.27.0

# Phase 8: MCP integration (official SDK; stdio server over the service layer)
mcp==2.1.1
