"""RevenueGuard backend: PostgreSQL persistence + FastAPI service layer (Phase 6).

The backend depends on the agent package's investigation pipeline; the agent
package never depends on the backend (import-boundary tested).
"""
